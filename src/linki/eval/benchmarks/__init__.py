"""Public-benchmark adapters for the eval harness.

Each adapter downloads a recognised benchmark's materials and maps them into
Linki's eval schema (``dataset.jsonl`` rows + an ingestible corpus). First one:
MIRACL (multilingual retrieval, 18 languages, native-speaker judged).
"""
