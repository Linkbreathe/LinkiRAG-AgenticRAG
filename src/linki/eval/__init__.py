"""Evaluation (Phase 6): prove the upgrade with numbers, not vibes.

A small dataset of the four question classes (chat / single / multihop /
out_of_kb) is run through both the naive baseline and the full agentic graph;
an LLM judge plus structural checks turn each answer into comparable metrics.
"""
